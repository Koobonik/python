title = "TEAMLAB X Inflearn"
print(title.count("a")) # 이 문자가 몇개 있는지
print(title.upper().count("a")) # 당연히 0 나옴
print(title.isdigit()) # 문자열이 숫자인지 판단 
print(title.startswith("a")) # 변수가 'a' 로 시작하는지 여부 판단